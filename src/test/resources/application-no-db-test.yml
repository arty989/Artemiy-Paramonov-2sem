server:
  port: 9091
spring:
  application:
    name: dbTestApp
  flyway:
      enabled: false
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none